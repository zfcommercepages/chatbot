Canonical **widget** assets. Served by FastAPI at `/widget/widget.css` and `/widget/widget.js` after deploy.

The separate **storefront-chatbot-netlify** repo pulls copies from here (via git submodule) for Netlify builds.
